import '../../core/localization/localized_material.dart';

import 'handwriting_note_screen.dart';

class NoteContainerScreen extends StatelessWidget {
  const NoteContainerScreen({super.key});

  @override
  Widget build(BuildContext context) => const HandwritingNoteScreen();
}
